{
  "name": "@ddays-app/types",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --external node",
    "dev": "tsup src/index.ts --format esm,cjs --dts --external node --watch"
  },
  "devDependencies": {
    "tsup": "^6.2.3",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "class-validator": "^0.14.0"
  }
}