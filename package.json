{
  "devDependencies": {
    "eslint": "^8.55.0",
    "turbo": "^2.2.3"
  },
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:generate": "yarn workspace api run db:generate",
    "db:migrate": "yarn workspace api run db:migrate",
    "db:studio": "yarn workspace api run db:studio"
  },
  "dependencies": {
    "qrcode.react": "^4.2.0"
  },
  "name": "ddays-app"
}
