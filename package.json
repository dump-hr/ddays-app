{
  "devDependencies": {
    "turbo": "^1.10.16"
  },
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "yarn workspace api run start",
    "introspect": "yarn workspace api run introspect",
    "generate": "yarn workspace api run generate",
    "migrate": "yarn workspace api run migrate"
  }
}